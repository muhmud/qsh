#!/bin/bash

CLIENT_OPTIONS_FILE=$QSH_CLIENTS/mysql/client-options
[[ -s "$CLIENT_OPTIONS_FILE" ]] && CLIENT_OPTIONS="$(cat "$CLIENT_OPTIONS_FILE")";

cat <<EOF > "$CLIENT_CONNECTION_FILE"
[client]
password="$PASSWORD"
$CLIENT_OPTIONS
EOF

